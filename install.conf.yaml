- defaults:
    link:
      relink: true

- clean: ['~']

#  - link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/Nested1/Nested2/Nested3
#- shell:
# - [git submodule update --init --recursive, Installing submodules]
